spring:
  profiles:
    active: ${SPRING_PROFILES_ACTIVE:dev}
  docker:
    compose:
      enabled: false
  datasource:
    url: ${SPRING_DATASOURCE_URL}
    username: ${SPRING_DATASOURCE_USERNAME}
    password: ${SPRING_DATASOURCE_PASSWORD}

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false
    properties:
      hibernate:
#        dialect: org.hibernate.spatial.dialect.postgis.PostgisDialect
        format_sql: false
        jdbc:
          time_zone: UTC
    open-in-view: false
    database-platform: org.hibernate.dialect.PostgreSQLDialect

  liquibase:
    change-log: classpath:db/changelog/db.changelog-master.yaml
    enabled: true
  application:
    name: ${APPLICATION_NAME:HOSTELBOOKING}
  mail:
    host: ${MAIL_HOST:smtp.gmail.com}
    port: ${MAIL_PORT:465}
    username: ${MAIL_USERNAME:fallback-email@gmail.com}
    password: ${MAIL_PASSWORD:fallback-password}
    properties:
      mail:
        debug: false
        smtp:
          auth: ${MAIL_SMTP_AUTH:true}
          starttls:
            enabled: ${MAIL_SMTP_STARTTLS_ENABLED:false}
            required: ${MAIL_SMTP_STARTTLS_REQUIRED:false}
          ssl:
            enable: true
            protocols: ${MAIL_SMTP_SSL_PROTOCOLS:TLSv1.2}
        test-connection: ${MAIL_TEST_CONNECTION:false} # You might want to set this to false for production if it causes issues on startup
  task:
    execution:
      pool:
        core-size: 2
        max-size: 10
        queue-capacity: 100
      thread-name-prefix: MailSender-

jwt:
  secret: ${JWT_SECRET}
  tokenExpiration: ${JWT_EXPIRATION:1800000} # 30 minutes in milliseconds
  refreshTokenExpiration: ${JWT_REFRESH_EXPIRATION:7200000}
  cookieSecure: ${JWT_COOKIE_SECURE:false}
  cookieDomain: ${JWT_COOKIE_DOMAIN:localhost}
  cookieSameSite: ${JWT_COOKIE_SAME_SITE:LAX}

vapid:
  public-key: ${VAPID_PUBLIC_KEY}   # generate once with VapidKeyGenerator util below
  private-key: ${VAPID_PRIVATE_KEY}
  subject: ${VAPID_SUBJECT}

hostel:
  sweeper:
    rate: ${HOSTEL_SWEEPER_RATE}

cloudinary:
  cloud-name: ${CLOUDINARY_CLOUD_NAME}
  api-key: ${CLOUDINARY_API_KEY}
  api-secret: ${CLOUDINARY_API_SECRET}

springdoc:
  api-docs:
    enabled: true
  swagger-ui:
    enabled: true



# Server Configuration for Production
server:
  port: ${SERVER_PORT:8080}
  servlet:
    session:
      cookie:
        same-site: ${COOKIE_SAME_SITE:LAX}
        secure: ${COOKIE_SECURE:true}
        max-age: 1800 # 30 minutes
    context-path: ${SERVER_CONTEXT_PATH:/}
  compression:
    enabled: true
    mime-types: text/html,text/xml,text/plain,text/css,text/javascript,application/javascript,application/json
    min-response-size: 1024
  http2:
    enabled: true
  tomcat:
    max-connections: 10000
    connection-timeout: 20000
    max-http-form-post-size: 2MB
    max-swallow-size: 2MB
    threads:
      max: 200
      min-spare: 10

app:
  frontend:
    base-url: ${FRONTEND_BASE_URL}
    password-setup-url: ${FRONTEND_PASSWORD_SETUP_URL}
    verify-email-url: ${FRONTEND_EMAIL_VERIFICATION_URL}


logging:
   level:
     org.springframework.security.config.annotation.authentication.configuration.InitializeUserDetailsBeanManagerConfigurer: ERROR
    # org.springframework.messaging: TRACE
   # org.springframework.web.socket : TRACE

