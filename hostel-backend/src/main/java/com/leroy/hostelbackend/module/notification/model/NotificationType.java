package com.leroy.hostelbackend.module.notification.model;

public enum NotificationType {
    BOOKING_APPROVED, BOOKING_REJECTED, BOOKING_CANCELLED,
    CHECKIN_REMINDER, WAITLIST_PROMOTED, COMPLAINT_CREATED, COMPLAINT_UPDATED, GENERAL
}
